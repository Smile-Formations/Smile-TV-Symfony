{% extends 'base.html.twig' %}

{% block title %}Movies - {{ movie.title }}{% endblock %}

{% block body %}
    <div class="container-fluid p-4">
        <div class="card">
            <div class="card-header">
                <h1>{{ movie.title }}</h1>
            </div>
            <div class="card-body">
                <p>Released at {{ movie.releasedAt|date('d/m/Y') }}</p>
                <label>Genres:</label>
                <ul>
                    {% for genre in movie.genres %}
                        <li>{{ genre.name }}</li>
                    {% endfor %}
                </ul>
            </div>
        </div>
    </div>
{% endblock %}
